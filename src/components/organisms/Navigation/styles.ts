import styled from '@emotion/styled';

const BtnWrapper = styled.div`
  padding: 4vmin;
  display: flex;
  width: 100%;
  justify-content: space-between;
  svg {
    cursor: pointer;
    :hover {
      opacity: 50%;
    }
  }
`;
export { BtnWrapper };
