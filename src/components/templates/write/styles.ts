import styled from '@emotion/styled';

const Container = styled.div`
  width: 80vw;
  height: 100%;
  padding: 1rem 2rem;
`;

const EditorWrapper = styled.div`
  display: flex;
  align-items: center;
`;
const Wrapper = styled.div`
  display: flex;
  justify-content: space-between;
`;

export { Container, EditorWrapper, Wrapper };
